import { init } from '../index';

test('accounts', () => {
    const publicKey = '';
    const privateKey = '';
    const facilitatorID = '';
    init(publicKey, privateKey, facilitatorID);

    // moov.accounts.create()


    
    // expect(moov.config.publicKey).toBe(publicKey);
    // expect(moov.config.privateKey).toBe(privateKey);
    // expect(moov.config.facilitatorID).toBe(facilitatorID);
});